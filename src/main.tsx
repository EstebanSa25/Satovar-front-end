import React from 'react';
import ReactDOM from 'react-dom/client';
import SatovarApp from './SatovarApp';

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <SatovarApp />
    </React.StrictMode>
);
